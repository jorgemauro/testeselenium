{
  "name": "teste-cert-node",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.10.1",
    "react-dom": "^16.10.1",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "s-test": "mocha ./src/test/index.js --timeout 20000",
    "selenium": "mocha  ./src/test/index.js --timeout 10000 --bail --compilers js:babel-core/register --require babel-polyfill "
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "chromedriver": "^119.0.1",
    "geckodriver": "^1.18.0",
    "mocha": "^6.2.1",
    "selenium-webdriver": "^4.0.0-alpha.5"
  }
}
